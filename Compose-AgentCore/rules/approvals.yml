# approvals.yml
# Gates manuales para promociones a QA/PRD.

.manual_qa:
  rules:
    - if: '$ENVIRONMENT == "qa"'
      when: manual
      allow_failure: false

.manual_prd:
  rules:
    - if: '$ENVIRONMENT == "prd"'
      when: manual
      allow_failure: false
